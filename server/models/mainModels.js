const {moviesModel} = require('./Movie');
const {seriesModel} = require('./Series');
const {imageYearsModel} = require('./imageYears');
const {imageModel} = require('./imageModel');

module.exports = {
    moviesModel,
    seriesModel,
    imageYearsModel,
    imageModel,
}